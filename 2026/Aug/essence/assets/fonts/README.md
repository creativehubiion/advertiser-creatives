# fonts/

Optional. Drop a `font.ttf` here to use a custom font. The `@font-face` rule in
`index.html` already points at `./assets/fonts/font.ttf`, and the scenes fall
back to Arial / sans-serif if it is missing, so the game runs either way.
